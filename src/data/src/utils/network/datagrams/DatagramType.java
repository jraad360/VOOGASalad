package utils.network.datagrams;

/**
 * Identifying enum for which subclass a Datagram is.
 * @author Jake Mullett
 */
public enum DatagramType {
    // Should only be requests and responses.
    REQUEST, RESPONSE
}
